@model IEnumerable<Prodaja_i_Servis_Racunarske_Opreme.Models.StrucnaSprema>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Naziv)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Naziv)
            </td>
            <td>
                @Ajax.ActionLink("Edituj", "Edituj_SP", new { id = item.Id }, new AjaxOptions { UpdateTargetId = "AjaxDiv" }) |
                @Ajax.ActionLink("Brisanje", "Brisanje_SP", new { id = item.Id }, new AjaxOptions { UpdateTargetId = "AjaxDiv" })
            </td>
        </tr>
    }

</table>

<p>
    @Ajax.ActionLink("Nova Strucna Sprema", "Dodaj_SP", new AjaxOptions { UpdateTargetId = "AjaxDiv" })
</p>
<div id="AjaxDiv"></div>