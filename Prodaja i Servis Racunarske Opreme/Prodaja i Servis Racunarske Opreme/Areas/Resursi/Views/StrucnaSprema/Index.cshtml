@model IEnumerable<Prodaja_i_Servis_Racunarske_Opreme.Models.StrucnaSprema>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Naziv)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Naziv)
            </td>
            <td>
                @Html.ActionLink("Edituj", "Edituj_SP", new { id = item.Id }) |
                @Html.ActionLink("Brisanje", "Brisanje_SP", new { id = item.Id })
            </td>
        </tr>
    }

</table>

<p>
    @Html.ActionLink("Nova Strucna Sprema", "Dodaj_SP")
</p>